{
  "name": "lebanon-electoral-law-simulator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/write-build-meta.mjs && vite build && node scripts/finalize-build.mjs",
    "preview": "vite preview",
    "start": "vite preview",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
